import { Node, Link, Time, DynamicGraph } from "./dynamicgraph";
/**
 * Calculates an ordering for the passed graph and time span
 * @param  {DynamicGraph} graph [description]
 * @param  {OrderingConfiguration} config  [description]
 */
export declare function orderNodes(graph: DynamicGraph, config?: OrderingConfiguration): number[];
export declare class OrderingConfiguration {
    start: Time;
    end: Time;
    nodes: Node[];
    links: Link[];
    algorithm: string[];
    distance: string[];
    constructor(start: Time, end: Time);
}
